name: semb
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.5.0
      - transformers==4.49.0
      - lm_eval==0.4.8
      - accelerate==0.34.2
      - antlr4-python3-runtime==4.11
      - math_verify
      - sympy
      - hf_xet
      - pydantic==2.10.6
      - gradio
      - datasets
      - sentencepiece
      - protobuf

